import styled from "styled-components";

const HList = styled.div`
  display: flex;
  flex-direction: row;
  flex-grow: 1;
  border: 1px dotted #00f;
  padding: 12px;
  border-radius: 8px;
`;

export default HList;
